/// <summary>Which pane the plugin column is showing.</summary>
public enum PluginKind
{
    /// <summary>The queryable schema: sources, their members, and the enums they use.</summary>
    Schema,

    /// <summary>The queries this browser remembers.</summary>
    History
}
